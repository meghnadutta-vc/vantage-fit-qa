# Smoke Suite — Diary Module

> This file lives in the shared `dashboard/smoke/` folder alongside every other dashboard module's
> smoke suite (e.g. `summary-smoke.md`, `quick-add-smoke.md`, `challenges-smoke.md`), so a CI/automation
> run can gate on "all modules' smoke suites" as one common pass, separate from each module's deeper
> functional test-case file under `dashboard/<module>/test-cases/`.

**Environment:** Production — `https://fitvantage.vantagecircle.com/ng/fit/summary/diary`
**Account:** Demo/test tenant — CRUD is safe.
**Purpose:** Minimal P1 gate for the whole Diary module — every case here must pass before deeper
functional/UI testing (see `dashboard/diary/test-cases/diary.md`) is meaningful. Ordering: page-load/
render checks → date navigation → write-paths (with state-branch pairs where genuinely needed — see
below) → known-bug regression guards → responsiveness smoke.

**Executed:** 2026-07-16 → 2026-07-17 (session genuinely spanned a real midnight rollover; one
mid-session re-login was required after the browser session dropped, noted where relevant). Result:
**17 Pass, 1 Fail, 1 Pass-with-flagged-Note/Doubt.** Actual Result is filled in below; **Status is a
QA judgment call the human reviewer should confirm**, not a blind pass/fail stamp.

**⚠️ Material update to the Bug #1 reference below:** live testing this run directly contradicts the
originally-reported "cross-day misattribution" mechanism — see TC-008a/008b/008c and
`dashboard/diary/bug-logs/bug-log.md` for the full re-assessment. Treat Bug #1's write-up as
**pending correction**, not confirmed as originally filed.

**Reference for automation authors:**
- Route: `/ng/fit/summary/diary`
- Primary data API: `POST /vantagefit/api/v1/today/overview` (backs the entire page — one network
  assertion point can validate most card data in a single call)
- Date nav controls: "Previous day" button, non-interactive "Today"/date label, "Next day" button
  (disabled on Today)
- **The page's real scroll container is Angular Material's `.mat-drawer-content`, not
  `document.body`/`window`.** Standard `fullPage` screenshots and `window.scrollTo()` will not reach
  content below the first viewport — target `.mat-drawer-content.scrollTop` instead.
- Known existing defect this suite guards against regressing further: Bug #1 (Water intake) — **now
  understood to be narrower than originally filed**: a live, reproducible fl-oz/L unit-label mismatch
  and a modal-goal-vs-card-goal mismatch (2000ml vs. "2.5 L"), NOT a cross-day data-attribution bug as
  originally claimed. See `dashboard/diary/bug-logs/bug-log.md`.

**Branching policy (why some cases are lettered, most aren't):** a case gets split into `a`/`b`/`c`
siblings **only when the expected result itself qualitatively changes** by ambient account state —
different button label, different pre-fill text, or a known bug's specific signature that only
ambient (not seeded) data can expose. That applies to Water (a live bug makes "is it zero or not"
genuinely unpredictable, and the correct check differs by which state you're in) and to Sleep/Weight/
Mood's create-vs-edit paths (label/pre-fill text is categorically different, not just a number).
Everywhere else — cards rendering, the Activities counter, Calorie Ledger arithmetic — the assertion
logic is identical whether the underlying numbers are zero or not, so those stay as **one row that
reads and asserts against whatever the actual state is at run time**, instead of being pre-forked.
Pre-forking those would have doubled case count without adding real coverage.

Status legend: ✅ Pass · ❌ Fail · ⚠️ Pass with flagged Note/Doubt · Priority is severity-if-failing.

| Test Case ID | Description | Preconditions | Steps | Expected Result | Actual Result | Status | Priority |
|---|---|---|---|---|---|---|---|
| DIARY-SMOKE-TC-001 | Diary page loads successfully | Logged in, demo tenant | 1. Navigate to `/ng/fit/summary/diary` directly (fresh load, not SPA nav) 2. Observe page load and browser console | Page loads within a reasonable time (<5s on normal network), no infinite spinner, no unhandled console errors, no failed (4xx/5xx) requests to `today/overview` | Loaded in under 2s on fresh navigation; `today/overview` and all supporting API calls returned 200. Only console error present was a benign third-party `ReferenceError: fbq is not defined` from the GTM/Meta-pixel tracking script — unrelated to app code, not a real defect. | ✅ Pass | P1 |
| DIARY-SMOKE-TC-002 | Page header shows correct heading and real date | On Diary page, Today selected | 1. Read "Diary" heading 2. Read date subtitle (e.g. "Today · 15 July 2026") 3. Compare subtitle against actual system date (`new Date()`) | Heading reads "Diary"; subtitle date exactly matches the real current date, no off-by-one/timezone drift | Verified twice on two different real days this session: "Today · 16 July 2026" and, after the session's real midnight rollover, "Today · 17 July 2026" — both exact matches. | ✅ Pass | P1 |
| DIARY-SMOKE-TC-003 | All 9 cards render valid content without error, whatever the day's actual data state | On Diary page, any day (data-rich or empty — assertion adapts) | 1. Load Diary for Today 2. For each of Snapshot, Calorie Ledger, Food Log, Sleep, Intake, Distance, Activities, Vitals, footer chrome: confirm it shows EITHER real populated data OR its documented empty-state copy | Every card shows one of the two valid states — none blank, none showing a raw error/stack trace, none stuck in a perpetual loading skeleton, regardless of which state the data happens to be in | Verified on both a data-rich day (14 Jul: 7 activities, Sleep, Mood, Weight all populated) and empty days (13/16/17 Jul, before logging): every card rendered a valid populated or empty state, no blanks/errors on any day. | ✅ Pass | P1 |
| DIARY-SMOKE-TC-004 | Previous day navigation loads a different day without crash | On Diary page, Today | 1. Note today's header date subtitle 2. Click "Previous day" 3. Read the new header date subtitle | Header date subtitle updates to exactly the prior calendar date (deterministic, data-independent check); no crash/blank page. *(Secondary, best-effort: at least one card's value also visibly differs — but two adjacent days can legitimately share an identical value, e.g. both zero, so this alone is not a failure if the date itself changed correctly.)* | Header subtitle updated correctly on each successive click: 16→15→14 July, landing on the exact correct calendar date every time; card data also visibly changed each step (confirmed via API). | ✅ Pass | P1 |
| DIARY-SMOKE-TC-005 | Next day is disabled on Today | On Diary page, Today selected | 1. Inspect/attempt to click "Next day" | Control is disabled (non-clickable); no navigation occurs, no error | Confirmed disabled exactly on Today across two separate real "Today"s this session (16th and 17th); correctly re-enabled the moment a past day (15th) was selected. | ✅ Pass | P2 |
| DIARY-SMOKE-TC-006 | Returning to Today restores correct current data | On Diary page, navigated to a past day (per TC-004) | 1. Click "Next day" repeatedly until back on Today 2. Read header date subtitle 3. Compare all card values against the baseline captured in TC-002/003 | Header date subtitle exactly matches today's real date again (primary check); all card values match the original Today baseline exactly — no residual stale data from the visited past day | After navigating 16→15→14 July and back (2× Next day), header and all card values matched the original Today baseline exactly. Resting kcal/Balance had ticked up slightly (1,119→1,123 kcal) — consistent with the known, intentional live-RMR-climb behavior, not a restoration failure. | ✅ Pass | P1 |
| DIARY-SMOKE-TC-007 | Snapshot Steps/Active Minutes match Summary page for the same day | Diary and Summary both loaded for the same date | 1. Note Steps value/goal and Active Minutes value/goal on Diary's Snapshot card 2. Navigate to `/ng/fit/summary` 3. Compare Summary's Snapshot card values | Values are identical on both pages (same value, same goal, same %) — holds regardless of whether the values are zero or non-zero | Diary and Summary both showed Steps 0/10000 (0%) and Active Minutes 0/32 mins (0%) for the same day (16 July) — exact match. | ✅ Pass | P1 |
| DIARY-SMOKE-TC-008a | [Primary/self-seeded] Logging a known water amount reflects exactly, with no cross-day contamination | On Diary page, Today | 1. Open "Log water" modal 2. **If the modal's own prefill is unexpectedly non-zero** (a known symptom of Bug #1), note it but do not treat it as this case's failure — proceed 3. Use the glass stepper to set an exact known count, e.g. 3 glasses (750 ml), not relying on any prefilled value 4. Save 5. Inspect the `today/overview` API response for today | Intake card's Water row updates to reflect the newly-logged amount plus any genuine pre-existing same-day amount (i.e. old value + 750ml, not overwritten to just 750ml unless it was 0 before); the API response's water activity entry for today is timestamped with today's actual time, not a prior day's | Modal pre-fill was CORRECT this run (0 ml / 0 of 8 glasses / "2000 ml to goal" — not the previously-reported buggy "5000ml/20 glasses"). Logged exactly 3 glasses (750ml) via the stepper. Card updated to "25.36/2.5 L". Raw API confirmed the new `activitiesData` entry is `"Water (06:03 PM)"` — a genuinely fresh timestamp matching real test time, NOT a copy of 14th's (05:33 PM) or 15th's (11:55 AM) entries. **This directly contradicts Bug #1's original cross-day-misattribution narrative** — logging on Today correctly attributed to Today. The "25.36" matching 14th/15th's figures appears coincidental: 750ml converts to exactly 25.36 fl oz regardless of day, and 750ml is evidently a round test amount reused across sessions. See bug-log.md for the full reassessment. | ✅ Pass (materially revises known-bug context — see note) | P1 |
| DIARY-SMOKE-TC-008b | [Regression guard — ambient state: nothing logged] Water shows zero on a day with no water logged | **Detect first:** confirm via a day never touched (or check API shows no water activity entry for it) | 1. Load that day 2. Read the Intake card's Water row | Water row shows "0 / 2.5 L" (or the correct daily goal) — no non-zero carryover value from any other day | Verified on 13 July (original untouched day) AND independently reconfirmed on fresh 17 July (post-midnight, before any logging): Water correctly showed "0/2.5 L" both in the UI and via raw API (`rawValue: 0`). **This case now PASSES — contradicts this file's own prior assumption that it was "currently known FAILING."** | ✅ Pass (was previously assumed failing — re-tested clean) | P1 |
| DIARY-SMOKE-TC-008c | [Regression guard — ambient state: already logged] Water value shown must be attributable to *this* day, not a different one | **Detect first:** confirm this day already has a genuine water log of its own (e.g. from TC-008a) | 1. Read the Water row's value 2. Cross-check the `today/overview` API response's activity feed for a Water entry, and confirm its timestamp's calendar date equals the day being viewed | Displayed value matches a water-log entry whose own timestamp genuinely belongs to the viewed day — this is the specific signature that distinguishes correct behavior from Bug #1 (which shows a *different* day's value/timestamp verbatim) | Confirmed via API: today's fresh 750ml log carries a timestamp (06:03 PM) genuinely belonging to today. Separately observed (not this case's own action, discovered while investigating): 15 July's own stored record still independently contains a "Water (11:55 AM)/750 count" entry — now understood as likely a real, separately-logged 750ml entry from that day (same round test amount reused across sessions), not proof of ongoing cross-day corruption. | ✅ Pass | P1 |
| DIARY-SMOKE-TC-009a | [State: nothing logged yet] Logging sleep for the first time succeeds and updates in place | **Detect first:** confirm Sleep card shows "No Data" empty state for today | 1. Click "Add Sleep Data" 2. Enter start 9:00 PM / end 5:00 AM (8h duration) 3. Save | Sleep card updates immediately in place (no manual reload/nav-away needed) to show "8 hrs 0 mins"; value persists after a hard reload | Logged 9:00 PM–5:00 AM (8h) using the modal's own default values. Sleep card updated in place immediately to "8 hrs 0 mins / Total sleep duration" with no reload needed. | ✅ Pass | P1 |
| DIARY-SMOKE-TC-009b | [State: already logged] Editing an existing sleep entry overwrites rather than duplicating | **Detect first:** confirm Sleep card already shows a logged duration for today | 1. Note the current duration shown 2. Open the edit affordance, change to a distinct new duration (e.g. 6h 30m) 3. Save | Card updates in place to show exactly the new duration (6h 30m) — the old value is replaced, not shown alongside it as a second entry; persists after hard reload | **No edit affordance found.** Clicking the Sleep card's duration text/summary produced no response — no modal opened, no navigation occurred. Diary's Sleep card appears write-once from this entry point (or requires an undiscovered trigger, e.g. a hover-revealed icon not exposed to the accessibility tree). This is a new, distinct gap from the already-known "no edit/delete on Activities rows" pattern. | ❌ Fail — no edit affordance found | P1 |
| DIARY-SMOKE-TC-010a | [State: never logged] Logging weight for the first time succeeds and persists | **Detect first:** confirm Vitals' Weight row shows "--" / "Log weight" (no prior value, no "Same as last log" hint) | 1. Click "Log weight" 2. Enter a distinct test value, e.g. 70.0 kg 3. Save 4. Hard reload the page | Vitals card shows "70.0 kg" immediately after save, and still shows it after a hard reload; button now reads "Edit weight" | **Note:** modal pre-filled "74.8 kg / Your latest weigh-in" rather than a blank/zero state, even though the card itself showed "--" and the API's own rawValue was null for today (see Section I's TC-016 question — modal "knows" a last value the card doesn't surface). Adjusted to 74.7kg via the stepper (0.1kg/click) and saved; card updated in place immediately to "164.69 lbs" (exact conversion), button switched to "Edit weight." | ✅ Pass (save mechanics correct; modal-prefill-vs-card-state inconsistency flagged separately) | P1 |
| DIARY-SMOKE-TC-010b | [State: already logged] Editing weight correctly pre-fills the last value and replaces it | **Detect first:** confirm Vitals' Weight row already shows a value and "Edit weight" button | 1. Open "Edit weight" 2. Confirm the modal pre-fills the existing value (and "Same as last log" hint, if applicable) 3. Change to a new distinct value 4. Save | Modal correctly pre-fills the prior value before editing; after save, card shows only the new value (old one is replaced, not duplicated) | Edit modal correctly pre-filled 74.7kg with "Same as last log" hint. Changed to 74.8kg via "+" stepper, clicked "Update weight" (correct label). Card updated to "164.91 lbs" — value replaced, not duplicated. Minor observation: 74.8kg → 164.91 lbs is ~0.03 lbs off the standard 2.20462 conversion factor's 164.95 — small rounding-order discrepancy, not a functional concern. | ✅ Pass | P1 |
| DIARY-SMOKE-TC-011a | [State: never logged] Logging mood for the first time succeeds and reflects immediately | **Detect first:** confirm Vitals' Mood row shows "--" / "Log mood" | 1. Click "Log mood" 2. Select a mood option, e.g. "Good" 3. Save | Vitals card's Mood row updates immediately to show "Good", button label switches to "Edit mood" | Selected "Pretty Nice." Mood row updated in place immediately, button switched to "Edit mood." **Discovered an undocumented feature:** selecting a positive mood surfaces a follow-up "Nice — what made it good?" tag picker (Exercise/Education/Family/Food/Friends/Relationship/Work/Travelling/Other) — worth adding to future Vitals test coverage. | ✅ Pass | P2 |
| DIARY-SMOKE-TC-011b | [State: already logged] Editing mood correctly pre-selects the current value and replaces it | **Detect first:** confirm Vitals' Mood row already shows a logged mood and "Edit mood" button | 1. Open "Edit mood" 2. Confirm the previously-selected mood option is shown pressed/selected in the modal 3. Select a different mood, e.g. "Not Good" 4. Save | Modal correctly pre-selects the current mood before editing; after save, card shows only the newly-selected mood (replaced, not duplicated) | Edit modal correctly showed "Pretty Nice" as pressed/selected. Changed to "Okay," clicked "Update" (correct label, not "Save"). Mood row updated to "Okay" — replaced, not duplicated. | ✅ Pass | P2 |
| DIARY-SMOKE-TC-012 | Activities "N logged" counter matches the actual list length, whatever N is | On Diary page, any day | 1. Read the "N logged" counter (or empty-state copy if 0) 2. Count the actual rows rendered below it | Counter number exactly equals the number of activity rows shown; if 0, correct empty-state copy is shown with zero phantom rows — holds whether the day has activities or not | Verified across every day checked: "No activities logged" (0 rows) on 13/16/17 Jul; "7 logged" on 14 Jul (7 real activity-type entries in the API, correctly excluding Track Mood/Water/Weight/Smoke Log which are separate metric logs, not activities); "1 logged" on 15 Jul (Hiking). Counter matched actual row count exactly every time. | ✅ Pass | P1 |
| DIARY-SMOKE-TC-013 | Calorie Ledger arithmetic holds, whatever the values are | On Diary page, any day | 1. Read Meals, Resting, Active, Balance values | Meals − Resting − Active = Balance exactly, including the trivial 0 − 0 − 0 = 0 case, and allowing for the intentional live-climbing Resting value at the moment of reading | Verified exact arithmetic on every day checked: 14 Jul (0−1494−1183=−2677 ✓), 15 Jul (0−1494−384=−1878 ✓), 16 Jul (multiple readings as Resting climbed live, always exact), 17 Jul (0−668−0=−668 ✓). No exceptions found. | ✅ Pass | P1 |
| DIARY-SMOKE-TC-014 | Page is usable at mobile viewport without layout breakage | On Diary page | 1. Resize/emulate viewport to 390×844 (mobile) 2. Scroll through the full page | No horizontal scrollbar/overflow; no card content overlapping another element; all card headings and primary values remain legible | No horizontal overflow at 390×844; all cards stacked cleanly and remained legible; header nav confirmed sticky (stayed visible while scrolled to the bottom). Two findings: (1) the real scroll container is Angular Material's `.mat-drawer-content`, not `document.body`/`window` — noted in the automation reference above; (2) the Freshchat "Chat with us" widget renders at 0×0 dimensions on this mobile viewport — unclear if this is an intentional design choice (ceding screen space to the bottom tab bar) or a functional regression blocking mobile-web chat access; flagged as Note/Doubt needing product confirmation, not asserted as a bug. | ⚠️ Pass (layout) with flagged Note/Doubt (chat widget) | P1 |
| DIARY-SMOKE-TC-015 | No unhandled errors during a full smoke pass | Throughout whichever branch cases above were executed | 1. Keep browser console and network tab open for the entire run | Zero uncaught JS exceptions; zero unexpected 4xx/5xx responses from app APIs (excluding any already-known, separately-filed backend bug like #1's misattributed data, which is a 200 with wrong data, not an error response) | Only console error across the entire session (multiple page loads, dozens of API calls inspected) was the benign third-party "fbq is not defined" (Meta/GTM pixel script) — zero application-code errors, zero unexpected 4xx/5xx from any `vantagefit/api` endpoint. | ✅ Pass | P2 |

---
**Not covered by smoke (see `dashboard/diary/test-cases/diary.md` instead):** Food Log/meal logging
(app-only hand-off, not web-testable), Distance card (no known web write-path), full accessibility
pass, full responsiveness matrix (desktop/laptop/iPad breakpoints beyond the single mobile check
above), multi-date regression beyond one Previous/Next round-trip, full negative/edge input-value
sweep (this suite only seeds one valid known value per field — invalid/boundary inputs belong in the
module file's Functional table under each card's "corner cases" section).

**New items surfaced this run, not yet in `diary.md` — worth adding when Section I/E are executed:**
- Sleep card has no discoverable edit affordance from Diary (TC-009b failure)
- Weight modal pre-fills a "last known value" the card itself doesn't surface (10a note) — direct
  evidence for Section I's TC-016 open question
- Mood's positive-response follow-up tag picker ("what made it good?") is undocumented in Section I
- Freshchat widget is 0×0 on mobile viewport — needs product confirmation, not yet a filed bug
- `.mat-drawer-content` is the real scroll container — needed for any automation targeting full-page
  content or screenshots
