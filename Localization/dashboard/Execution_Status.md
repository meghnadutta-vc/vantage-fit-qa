# Execution Status — Vantage Fit Dashboard Localization

> Updated after every execution. Tracks per-module progress through the 4 phases.

| Module | Server | Language(s) | Phase 1 (Design) | Phase 2 (Execute) | Phase 3 (Bugs) | Phase 4 (Report) | Overall |
|---|---|---|---|---|---|---|---|
| Overview | India | en/de/fr/es | ✅ (44 cases) | ✅ (+UI-break pass 1440/1366/1024) | ✅ (**11 bugs** — 4 new: OV#8–#11) | ✅ | ✅ Done (UI-break pass 2026-07-28) |
| Create Challenge | India | en/de/fr/es | ✅ (27 cases) | ✅ (full flow + publish) | ✅ (5 module bugs + 2 cross-module) | ✅ | ✅ Done (minor gaps: fr/es deep steps, toast text) |
| Manage Challenges | India | en/de | ✅ (18 cases) | ✅ (listing, edit, view) | ✅ (2 module bugs + cross-module) | ✅ | ✅ Done (delete/archive UI absent; toast/tabs NV) |
| Past Challenges | India | en/de | ✅ (11 cases) | ✅ (listing + view) | ✅ (0 module bugs; cross-module only) | ✅ | ✅ Done (clean) |
| Reports (all 6) | India | en/de | ✅ (19 cases) | ✅ (all 6 + data re-run) | ✅ (5 module bugs + cross-module) | ✅ | ✅ Done (grouping/redemption values NV) |
| Configuration → Settings | India | en/de | ✅ (29 cases) | ✅ (statics + save bar + remove + tooltip + validation) | ✅ (0 module bugs; SET#1/#2 observations) | ✅ | ✅ Done (CLEAN; fr/es + upload-error NV) |
| Programs → Content Library | India | en/de | ✅ (20 cases) | ✅ (statics + filters + table + summary + dict lookup) | ✅ (3 bugs P2×1/P3×2 + CL#4/#5 obs) | ✅ | ✅ Done (read-only; search/filter-apply/row-menu + fr/es NV) |
| Community → Events | India | en/de | ✅ (21 cases) | ✅ (View Events 3 tabs + Create Event form + pickers + audience) | ✅ (2 bugs P2×1/P3×1) | ✅ | ✅ Done (read-only; validation/submit-toast/detail + fr/es NV) |
| Programs → Create Content | India | de | ✅ (8 cases) | ✅ (type-picker + Linked Content + Bite-Size builder) | ✅ (2 bugs P2×2) | ✅ | ✅ Done (Linked form localized; picker+builder EN not-externalized) |
| Community → Create Announcement | India | de | ✅ (11 cases) | ✅ (landing + create form) | ✅ (2 bugs P2×2) | ✅ | ✅ Done (full de dict exists but not consumed — wire-up) |
| Communications → Publish Notifications | India | de | ✅ (8 cases) | ✅ (content + audience + preview) | ✅ (0 bugs) | ✅ | ✅ Done (CLEAN) |
| Communications → Send Custom Email | India | de | ✅ (8 cases) | ✅ (page + email-template preview) | ✅ (1 obs P3) | ✅ | ✅ Done (page clean; email template mixed-lang SCE#1) |
| Communications → Email Designer | India | de+en | ✅ (6 cases) | ✅ (modal re-tested 2026-07-28; route resolved) | ✅ (ED#1 P2 confirmed: de/en pixel-identical) | ✅ | ✅ Done (100% EN not-externalized; 0 UI overflow) |
| Workforce Health (3 pages) | India | de | ✅ (13 cases) | ✅ (Wellness Score + Leagues; Health Insights blocked) | ✅ (WS#1 P2 + WL#1 P3 + RPT#1/#2 cross) | ✅ | ✅ Done (Health Insights ⛔ iframe; export/dropdowns + fr/es NV) |
| Rewards → Upload Points | India | de | ✅ (8 cases) | ✅ (full form + steps) | ✅ (0 bugs) | ✅ | ✅ Done (CLEAN; upload/toast + fr/es NV) |
| Configuration → Add Employees | India | de | ✅ (7 cases) | ✅ (instructions + note + dropzone) | ✅ (1 bug P3) | ✅ | ✅ Done (AE#1 dropzone EN; upload/toast + fr/es NV) |
| Configuration → Preview Emails | India | de | ✅ (7 cases) | ✅ (chrome + 9 email-type cards) | ✅ (1 bug P3) | ✅ | ✅ Done (PE#1 card titles EN; save-toast + fr/es NV) |
| French & Spanish pass (cross-module) | India | fr/es | ✅ (13 cases) | ✅ (dict parity + Overview/Content Library/Settings spot-checks) | ✅ (FR#1 P3 new; all de bugs reproduce) | ✅ | ✅ Done (dicts complete 991/991; exhaustive per-module + dynamic NV) |

## Legend
- ☐ Not started · ◐ In progress · ✅ Done · ⛔ Blocked · ⏸ Paused

## Run history
- 2026-07-29 (Run 16) — **ALL 18 SHIPPED LANGUAGES NOW TESTED.** Docs: `bugs/logs/all-18-languages.md`. Covered the 10 remaining (`or hi ru ko vi hu nl it id fr-CA`) at 1440 across 4–6 representative modules each; all 10 dictionaries complete (0 missing, 0 empty). **NEW FRCA#1 [P3] — fr-CA partially falls back to metropolitan French:** an fr-CA session renders `RÉPARTITION DU SCORE`, a string that exists **only in `fr.json`** (fr-CA specifies *"Répartition du pointage"*, English is *"Score Breakdown"*), so it cannot be an English leak — the UI resolved the fr value in an fr-CA session. Partial, not total: `main-d'œuvre` and `Balados` do render, while the Content Library type filter shows `Podcast` where fr-CA specifies `Balado`. fr-CA is genuinely translated (42 keys differ from fr with correct Québec terms), so paid-for work is not reaching users. **NEW HU#1 [P3] — Hungarian gives the worst overflow in the engagement:** `Alkalmazotti azonosító` needs **+119px more than a 110px box**, over double the container. **NEW RU#1 [P3]** — Russian has the worst break count at 1440 (8 on Overview vs 0–4 elsewhere). **OV#7 reproduced with cross-language evidence:** an in-place Italian→Indonesian switch left the chip showing Italian `Tutte le fasce d'età` while the rest of the page was Indonesian with `fit_lang=id`; a cold load then showed the correct `Semua Kelompok Usia` — two languages on one screen, the clearest reproduction yet, re-confirming "verify on a fresh load". **U7#3 now confirmed in FOURTEEN languages** — the date affix localizes correctly every single time (de `Am`, es `El`, fr `Au`, pt `Em`, pl `Na dzień`, zh `截至`, ru `На`, vi `Vào`, nl `Op`, it `Al`, id `Per`, and postpositionally or `…ରେ`, hi `…को`, hu `…napon`) while the month stays English; strongest possible proof the translation layer works and the date layer does not, and one formatter fix clears U7#1+U7#3+RPT#4+CC#2+AR#2 across all languages. **Script rendering passes in all 18** — zero tofu/mojibake including Odia and Devanagari (the highest font-risk candidates, 526 strings each, conjuncts correct), Cyrillic, Hangul, Han, Arabic shaping and heavy-diacritic Latin. **Text-expansion ranking: `hu +119` › `ru +68` › `pl +65` › `de +62` › es › pt › fr/nl/it/or › hi › id › `zh +28` › `ko +13` — German ranks 4th, so Hungarian and Russian should be the layout-stress references.** Scope: 1440 only for the 10 new languages, 4–6 modules each; F1–F8 functional remains German-only engagement-wide.
- 2026-07-28 (Run 14–15) — **Enriched checklist completed for all six languages; then ARABIC tested for the first time.** Docs: `bugs/logs/full-checklist-2res-run13.md` (Run 14 section), master bug log Arabic section. **Run 14:** re-ran the invalidated Chinese pass (now valid — 902 leaf nodes vs near-zero during the outage) and completed es/fr/pt/pl, so **de/es/fr/pt/pl/zh-CN all have the enriched checklist**. **Corrected two of my own detector flaws:** the leaf-count load guard produced a **false INVALID** on legitimately sparse report pages (replaced with a chrome-presence check — this flaw withheld valid findings, it never invented clean ones), and the U3 bleed detector produced **false positives**, flagging `Próximo`/`Valor` as Portuguese bleed in a Spanish session when both are the correct Spanish values that merely coincide with Portuguese (fixed by excluding values present in the active dictionary; **neither was reported as a bug**). **New U7#3:** the Wellness Leagues as-of date is a mixed-language fragment in **all six languages** — the prefix localizes perfectly (`Am` / `El` / `Au` / `Em` / `Na dzień` / `截至`) while the month stays `Jul`; because the surrounding words prove the i18n wiring on that element is correct, the defect is unambiguously the **date formatter**, and fixing that one formatter resolves U7#1 + RPT#4 + CC#2 across six languages at once. RPT#7 confirmed cross-language (Chinese empty state also instructs clicking a non-existent "生成" button). Confirmed language-independent by measuring all six: `<html lang>="en"`, images without `alt` (103/24/1), report column-selector clip, English dates. Clean in all six: raw keys, placeholders, bleed, mojibake. **Run 15 — A1 + Arabic:** **A1 locale propagation PASSES** — `accept-language: pl` is correctly sent on API calls, which means the backend receives the locale and the [BE] English strings are a **backend-scope decision, not a missing-header bug**. **AR#1 [P2] — Arabic is fully translated but rendered LEFT-TO-RIGHT: RTL is not implemented at all** (`<html dir>` absent, `body`/`main` computed `ltr`, **0** elements with `dir=rtl`, sidebar still on the left at x=80, "view more" arrows still pointing right). Arabic text merely *looks* partly right because the browser applies bidi within text runs. Arabic ships a complete 991-key dictionary and is live in the language list, so an entire locale is paid-for and structurally unusable — logged P2 by the letter of the scale (no crash/data loss) but flagged as a **market-readiness blocker needing a product decision on whether Arabic should remain selectable until RTL ships**. Also **AR#2** mixed fragment `آخر 30 يومًا | Jun 28, 2026`, **AR#3** numeral systems mixed within one row (`٢٠٪` beside `89` — the Arabic-Indic digits are baked into translation strings, so not fixable in the formatter alone; needs a product decision), **AR#4** `<html lang>="en"` in Arabic too (worst here, since a screen reader needs both language and direction).
- 2026-07-28 (Run 12) — **FRENCH + PORTUGUESE + POLISH + CHINESE (zh-CN): layout + strings, ~23 surfaces each @1024.** Docs: `bugs/logs/multilang-fr-pt-pl-zh.md`. Completes the requested six-language set (de/es were already done). **Precondition verified first:** all **18** selector languages resolve to a complete **991-key** dictionary (200 OK) with **0 missing keys and 0 empty values**; identical-to-EN is only 0.4–3.4% and all legitimate cognates/brand/placeholders — so **there is no "missing translation" defect class**, every finding is wire-up / not-externalised / [BE] / formatting / layout. **NEW OV#12 [P2] — the Workforce Health Snapshot / Wellness Tiers card renders entirely in English in ALL SIX languages** despite full translations existing (Wellness Tiers → Paliers de bien-être / Níveis de bem-estar / Poziomy dobrostanu / 健康等级; Gold → Or/Ouro/Złoto/黄金). One card, ~7 keys, and fixing it closes a leak **and** a layout break in six languages; the `+122px` `.tiers-card` spill is **identical in all six including Chinese**, which proves the overflow is caused by the untranslated English rather than text expansion. **OV#12 subsumes OV#10 and OV#11** — same unwired card. **CORRECTION to Run 7: EV#4 is NOT German-specific.** Event tabs clip in **pl +177 (worst) / fr +136 / pt +127 / de**, while only **es (+2) and zh (0)** escape — so it is an all-long-language bug and its priority rises. **SET#3 is worst in French** (+87 > de +73 > pt +68 > en +67 > es +60 > pl +42 > zh 0), so "German is longest" keeps failing as a scoping heuristic. Add-Employees header worst in Polish (+356). Report column-selector clips **+31/+48 identically in all six** because the chip renders English while the header beside it is translated (ES#3 = language-independent). **Chinese established as a diagnostic:** 8 breaks vs 17 on Overview and 0 on Events/Settings — CJK compresses, so **the breaks surviving in Chinese are exactly those NOT caused by translation length** (untranslated English or structural/responsive). CJK glyphs, Polish diacritics and PT/FR accents all render clean — no tofu, no mojibake. **French-only layout consequence:** the audience operator localizes to "est dans" and then CLIPS (+6px in a 50px box) where de/es leave English "is in" and fit — the ES#4 pattern again, so fixing that wire-up requires widening the box first. Leak inventory identical across the four: RPT#1, ES#3, CL#1 (Capsule/Cápsula/Pigułka/播客), AE#1, Reward, Title, Male, CC#1 template names, CRC#1. **Scope: 1024 only** — 1920/1366/768/375, dynamic flows/CRUD (G15), and per-locale date/number formatting remain untested for these four; the other **12 selectable languages also ship complete dictionaries and are entirely untested (Arabic/RTL highest risk)**.
- 2026-07-28 (Run 11) — **THE P1 HUNT EXECUTED: G5 + G6 + G4.** Docs: `bugs/logs/p1-hunt-g5-g6-g4.md`, test files in `test-data/`. These were the engagement's only three credible P1 leads and the sole reason it reported zero P1s. **Verdict: none is a P1.** **G5 resolved & downgraded to P3** — the CSV path is SAFE (server explicitly rejects `12,5`: *"Award Amount not found or not an integer"*, while the `1000` row drew no amount error), so the data-corruption scenario does not occur; the residual is the `type=number` input path where the **browser** strips the comma (`12,5`→`125`, `valid:true`, no error), but the only affected field is an integer with `max=500`, so impact is bounded and no field was found where locale decimals silently corrupt meaningful data. **G6 PASSES outright** — UTF-8 non-ASCII (`Jürgen Müller`, `Renée Nuñez`, `Zdenek Šimek`, `Außergewöhnliche Leistung`) renders with **no mojibake**, and the **semicolon delimiter German Excel emits by default is auto-detected** and parsed into 8 correct columns (re-verified with a uniquely-marked row after spotting that my first apparent pass could have been a stale preview). **G4 ⛔ BLOCKED on test data** — Employee, Redemption and League reports all return **zero rows** on default filters so no file with content can be exported (League has no Export control at all); export on an empty table fires no request and shows no message. Partial G4 finding: the downloadable **sample CSV template has English headers** in a German session and no UTF-8 BOM — and since the parser matches those English headers, localizing them would break upload unless both sides change together (**product decision**). **So "zero P1s" now reflects testing rather than absence of testing** — but the run produced **2 new P2s that are more actionable than the leads I was chasing**: **UP#4 [P2] a 400 from the upload endpoint produces NO user feedback at all** (server returned a detailed per-row error body; UI showed nothing — silent failure on a data-writing operation, and the frontend is simply discarding a good payload; note client-side validation *does* toast, so error handling is inconsistent); **UP#5 [P2] the upload preview accumulates instead of replacing** (2 stacked tables confirmed — on the exact upload→fix→re-upload path the admin cannot tell which data will submit, on a screen that grants points). Also **UP#6 [P3]** first error-state string ever captured in this engagement (G8 was at zero) and it failed — *"Error / Please select a country"* hardcoded English, no key in any of the 4 dictionaries, despite the module's own `pointsUpload.*` namespace being translated; **UP#7 [P3]** required field did not gate submit (`aria-disabled=false` with empty required field — deviates from the documented preventive-validation pattern); **RPT#7 [P3]** empty state instructs *"klicken Sie auf Generieren"* but **no Generieren button exists** (all 45 visible controls enumerated). Positives recorded: German empty states are correctly localized and natural; UTF-8 handling is solid end-to-end; backend numeric validation is strict. **Blast radius zero** — the 400 rejected every row so no points were granted; all test rows used the reserved `example.invalid` domain by design; no test-data debt created.
- 2026-07-28 (Run 10) — **DESKTOP 1920×1080 sweep, German + Spanish, all modules + CRUD.** Docs: `bugs/logs/desktop-1920-de-es-crud.md`. Overflow **classified** this time (CLIP / SPILL / SCROLL) so `overflow:auto` data tables stop counting as bugs. **Headline: at 1024 I found breakage in 15 of 17 modules; at 1920, across 23 surfaces per language, only 3 components break — and all 3 are fixed-width boxes** (`.notif-title` 150px, report column-selector 150px, Wellness Leagues chips 110/100px). **OV#8, OV#9, OV#11, EV#4, SET#3, MGC#3, CC#6 do NOT reproduce at 1920** → the Run-5 P3 pile is **viewport-dependent, not desktop-facing**, and should be triaged as "small laptops / split-screen", a materially lower priority than the raw count implied. The fixed-width trio clips at **every** resolution and is the group actually worth fixing first. **EV#4 re-rated DOWN**: I had flagged it as a possible functional P2 (tabs unreachable); at 1920 the tabs fit, so it stays P3 confined to narrow viewports. Broken images unchanged (5 / 12 / 1) — resolution-independent, as expected for a CDN defect. **CRUD: G5 comma-decimal CONFIRMED** — typing `12,5` in German stores **`125`** with `valid:true`, `badInput:false`, **no error**; typing it into a field holding `500` gave **`500125`** (comma dropped, input appended). Mechanism proven; **impact on a decimal-legitimate field still untested**, which is what decides P1 vs P2 — logged as the top open item. **UPDATE verified twice and reverted** (Settings, incl. a working `beforeunload` unsaved-changes guard; Preview Emails toggle → correctly localized "8 von 9 aktiviert" → restored to 9/9; no toast on toggle, contrary to prior assumption). **CREATE: wizard steps 1–4 walked, 0 layout breaks, F3 validation gating confirmed — publish deliberately NOT executed** because the audience resolved to **960 real UAT users**, exceeding blast-radius rules; verified nothing persisted (still 103 active challenges, no new debt). **DELETE: no delete control exists** on challenge cards at all (only Ansehen/Verwalten) — confirms G24 and explains why challenge 25441 is permanent; Enhancement, not defect. **New findings:** CC#2 German date picker fully English (`JUL 2026`, `Monday…Sunday`, initials `M T W T F S S` — even the initials are wrong); date format `30/07/2026` where German needs `30.07.2026`; CC#3 fuller inventory (`is in` ×4, `(+N others)` ×4, `All`, `Undisclosed`); **G13 terminology split CONFIRMED with evidence** — Preview Emails says *Herausforderung* while sidebar/wizard say *Challenge*; **first dashboard register defect** — "Erstellen *Sie Ihre* eigenen…" formal *Sie* against the informal *du* voice (B12 class). Activity master list (21 items) correctly classified **[BE] expected English** — 20 of 21 have no key in any dictionary; the endpoint re-fetch 401'd so the source is inferred, and whether activity names are in localization scope **Needs Product Confirmation**.
- 2026-07-28 (Run 9) — **Cold-load re-measurement of the 11 in-app-navigated Spanish modules** (direct URL, no clicking). **Result: 1 of 11 differed** — Participant Report gained `Active Users → Usuarios activos`; the other 10 render identically cold and warm. **This narrows ES#1 and corrects my own Run-8 framing:** I had written that ES#1 meant any module verified by clicking through the sidebar "may have been verified in its good state only", and flagged the German sign-offs as broadly suspect plus a German cold-load sweep as the highest-value remaining task. The measurement does not support that — **ES#1 is component-specific, not systemic** (Content Library type filters, Wellness Leagues filter chips, Participant Report `Active Users`), the German pass is **not** invalidated, and that task is downgraded to low value in `REMAINING_WORK.md`. ES#1 keeps **P2** (the cold state is what users land in). **New analytical outcome:** RPT#1 and ES#1 look identical on screen but are **two different defects** — RPT#1 is English in *both* cold and warm states (hardcoded default, never localizes), ES#1 goes Spanish once the i18n dictionary is warm (init-order race). Two separate fixes; a dev treating them as one bug will close half of it. All Spanish per-module counts are now **cold-load-verified totals**, no longer floors. Standing method rule adopted: verify localization by direct URL, never by clicking the sidebar.
- 2026-07-28 (Run 8) — **SPANISH full-module sweep: layout + string coverage together.** Docs: `bugs/logs/spanish-full-sweep.md`. Covered the 14 modules Run 7 missed, so Spanish now has **18 of 19 modules** measured (Health Insights ⛔; Create-Content and Email-Designer modals deduced-not-measured — they have **no i18n keys**, so no language can localize them). Leakage was detected rigorously: match visible strings against en.json values that have a *different* es.json value → every hit is a proven **[FE] wire-up** bug, not a missing translation. **4 new bugs.** **ES#1 [P2] cold-load renders shared filters in English** — cold load shows `All`/`All Countries`, in-app nav to the *same route* shows `Todos`/`Todos los países`; same URL, `fit_lang=es` throughout. Reproduced twice each on Content Library + Wellness Leagues. This is the **inverse of OV#7**, the broken state is the **default** state (bookmark/refresh), and it means **any module verified by navigating in-app may have been verified in its good state only** — a direct hit on earlier sign-offs (realises gap G1). Evidence `evidence/contentlibrary_es_coldload_filters_english.png`. **ES#2 [P3]** mixed-language filter row ("País: All Countries" beside a correct "Todos los grupos de edad"). **ES#3 [P3]** report column-selector shows `Date of Joining` while the table header on the *same screen* shows `Fecha de incorporación` — unambiguous wire-up proof, no missing-translation explanation available. **ES#4 [P3]** Wellness Leagues Spanish chips overflow (+72/+58/+11px) **only where the wire-up works** → **fixing RPT#1 will introduce this overflow across all 6 report surfaces; widen the chips before wiring translations.** Also confirmed reproducing in Spanish: RPT#1 (6 surfaces), CL#1 (Vídeo/Pódcast/Artículo/**Píldora**), AE#1, plus Reward/Title/Enrolled/Date; and CC#1 with full inventory (all 5 template titles **and** all 5 descriptions English while buttons are Spanish). Clean in Spanish: Preview Emails, Past Challenges, Create Event, Send Custom Email, builder step 1. Table `+334/+454/+1002` overflows are `overflow:auto` **scrollable** data tables — not bugs. Sidebar: all 24 labels localize. **Method caveat recorded:** 11 modules were batched via in-app nav, so per ES#1 their leak counts are **floors** — cold-load re-measurement is logged as open.
- 2026-07-28 (Run 7) — **SPANISH (es) layout pass**, targeted at the known German findings to get a 3-language comparison. Docs: `bugs/logs/ui-break-sweep-es.md`. **Key result: OV#8 was two conflated bugs.** At **1440** English AND Spanish both fit in exactly 113px while only German overflows → **OV#8a = German-specific localization bug**. At **1024** the box shrinks to 44px and **every language including English overflows** (+5/+18/+18/+4 in en) → **OV#8b = responsive bug, all languages**. Separate tickets/owners needed. **Spanish is NOT uniformly milder than German** — it is WORSE on 3 measurements: PN#1 `.notif-title` (es +8px vs de +3px, en fits exactly → reclassify PN#1 as "all non-English", Spanish worst), PN#1 `.two-column-layout` (es +516 > de +415 > en +381), and OV#9's Incentivización card (es +51px vs de +35px, since *Incentivación* > *Anreize*). So "test German because it's longest" is unsafe: German loses on compound nouns, Spanish on prepositional phrases. **EV#4 confirmed strongly German-specific** — Spanish tabs 496px in a 542px container (+2px, effectively fits) vs German 713px in 470px (+291px clipped). **Confirmed language-independent** (identical in all 3 languages): OV#10 "Last 30 Days" wire-up (so it is NOT a German bug — severity is higher than first logged), OV#11 tier row +122px, and the broken-image counts (5 on Manage Challenges, 12 on Events). SET#3 in Spanish ≈ English (5 patterns, banner-actions +60 vs en +67 / de +73). MGC#3 es +45px/65 cards (en +30/65, de +62/97). **Scope: layout only, 5 of 19 modules** (Overview, Manage Challenges, Settings, Events, Publish Notifications) — Spanish *string/translation* coverage across all modules remains unverified (G14) and Spanish dynamic flows remain untested (G15).
- 2026-07-28 (Run 6) — **English controls + Email Designer resolved.** (a) **Controls @1024** re-measured every sweep finding with `fit_lang=en`, which RE-CLASSIFIES them: **genuine localization bugs = 3** (OV#8 glance labels — en fits at exactly 113/113px with 0 headroom; EV#4 event tabs — en 449px fits a 542px container with 93px spare vs de 713px clipped 291px, and **no tab-pagination exists in either language** so the 3rd German tab is truly unreachable; PN#1 `.notif-title` — en fits exactly 150/150, de clipped +3px). **Responsive defects amplified by German = 3** (MGC#3 en +30px/65 cards vs de +62px/97 cards; SET#3 en 5 patterns vs de 6, biggest delta on toggle rows +6→+26/+30; AE#3 en +274 vs de +343). **NOT localization = 4** (CL#6 `.search-container` +200px identical both langs; UP#3 +474px identical; CC#6 hardcoded `grid-cols-2`; broken-image family — 5 broken on Manage Challenges in en too). **Correction to Run 5 framing:** SET#3/PN#1 were called out as contradicting a CLEAN sign-off — too strong. The prior CLEAN was about *translation* (correct: those German strings are all properly translated); the earlier pass simply never tested 1024 responsive, and those layouts break in English as well. (b) **Email Designer (module 15) NOW TESTED** — it is not a route (no `href`; opens the Rich Email Composer as a full-screen modal, URL unchanged), reachable via Communications group → "E-Mail-Designer". **ED#1 confirmed**: de and en screenshots are pixel-identical, 100% English, though the sidebar link itself IS localised → German menu item opens an English surface. 0 overflow at 1024, so re-run the sweep on it once localised. (c) **Two suspected bugs ruled out**: "unclickable sidebar item" was a collapsed accordion (`height:0; overflow:hidden` children still report non-zero rects); "invisible `.rec-host--headless` overlay" is the modal backdrop behaving correctly. (d) **Method caveat**: the sweep's `vis()` helper didn't reject elements collapsed by an ancestor, so some pattern *counts* may be inflated — all headline findings were screenshot-verified and stand; corrected helper recorded in `bugs/logs/ui-break-sweep-de.md`.
- 2026-07-28 (Run 5) — **ALL-MODULE German UI-break sweep @1024** (+1440/1366 spot-checks), per user request to emphasise UI breakage. Docs: `bugs/logs/ui-break-sweep-de.md`. **15 of 17 swept modules break in German at 1024.** 16 new bugs: **CC#6** template grid +151px (hardcoded grid-cols-2, no breakpoint; language-independent); **MGC#3** action row renders ~45px outside the card on ~97 cards (buttons verified still clickable → P3); **MGC#4 (P2)** 5 broken campaign banners; **PC#1/PC#2** broken image + long-title overflow +104px; **RPT#6** column selector clipped +31px (also still English = RPT#2); **SET#3 (P2)** Settings breaks badly — card titles cut, size chip shattered onto 4 lines, **"Entfernen" clipped to "× Ent"** — *module was previously signed off CLEAN*; **AE#3** +343px; **UP#3** +474px (largest); **CL#6** filter row +200px & "1 language" clipped; **EV#3 (P2)** 12 broken images; **EV#4 (P2)** German event tabs clipped — 713px of tabs in a 470px container, 3rd tab unreachable; **EV#5** +114px; **PN#1** two-column layout +415px — *also previously CLEAN*; **SCE#2** +441px (same shared component as PN#1); **WS#2** +20px; **WL#2** two selectors clipped incl. correctly-translated German "Alle Altersgruppen". **Cross-surface:** the `.png.png` broken-image pattern is the SAME defect as employee-web **B23** — 18 broken images on the dashboard + ~28 on the employee web = one shared upstream URL-construction bug. **Clean:** Preview Emails, Create Announcement, Create-Challenge builder Step 1. **NOT tested:** Email Designer (route unresolved), Create-Content picker modal, 5 of 6 report pages, widths 768/375, English controls for most modules (so German-vs-general-responsive split is unquantified outside Overview).
- 2026-07-28 (Run 4) — **Overview: German UI-break focused pass** (viewports 1440/1366/1024). Emphasis on layout breakage per user request. **4 NEW bugs:** **OV#8 (P2)** At-a-Glance metric labels overflow their fixed 113px box and render over the tile icon (Achtsamkeitsminuten +27px, Durchschnittlicher Schlaf +8px, Durchschnittliche Schritte +4px at 1440; +39/+20/+16 at 1366; +96/+77/+73 at 1024) — **English control measured exactly 113px in a 113px box = 0px headroom**, so the container is English-fitted by design and breaks in any longer language; **OV#9 (P2)** stat-card headers clip "Mehr anzeigen" mid-word at 1024 ("Me anzei"/"Meh anzeig", overflow +69/+63/+35px); **OV#10 (P3)** At-a-Glance subtitle renders English "Last 30 Days" on a FRESH load — proven wire-up because `.font-medium` (date filter) shows "Letzte 30 Tage" from key `subheader.presets.last_30_days` on the SAME load (explicitly NOT a re-open of OV#7, which concerned the date PRESET); **OV#11 (P3)** Wellness-Tiers tier row overflows (+8px @1366, +122px @1024). Re-confirmed OV#2/#4/#5, CL#4 (widget also overlays card content), SET#1. **Method correction:** the previous detector only flagged `overflow:hidden|clip`, but every finding here has `overflow-x: visible` (text SPILLS rather than clips) — detect via `scrollWidth > clientWidth` regardless of overflow property. Addresses gaps **G2** (visual review) and **G11** (responsive at localized lengths) for Overview only. Evidence: `india_overview_de_{1440_full,1440_glance_overflow,1024_break}.png`.
- 2026-07-22 (Run 3) — **Remaining success-path submits (UAT creation authorised, formal content names).** Renamed Content-Library test item → "Managing Workplace Stress: A Practical Guide" (update toast "Inhalt erfolgreich erstellt" localized ✓ but reuses create wording). Upload Points real upload (1 pt to self) → **UP#2 (P3)** success toast "Success — File uploaded" ENGLISH. Announcement published "Q3 Wellness Program — Now Live" (Atlanta/US) → publish toast "Success — Announcement creation in progress…" ENGLISH = **ANN#3 confirmed on publish**. Add Employees uploaded "QA Test Account" → **AE#2 (P3)** success toast "The file was successfully uploaded…15 minutes…" ENGLISH. Create Event NOT submitted (date fields calendar-only; components already verified; create-toast expected localized). Toast pattern: send/create/save localize; upload/announcement/loading toasts English. Test data on 355 documented (formal names).
- 2026-07-22 (Run 2) — **Dynamic-flow + FUNCTIONAL pass, German** (re-login as meghna.dutta after sessions expired). **Functional nav/redirect sweep: 24/24 PASS** (every sidebar link routes correctly). Create Content (Linked): create toast "Inhalt erfolgreich erstellt" LOCALIZED ✓, image crop dialog localized, form/upload/crop/submit functional; **DF#1 (P3)** generic loading toast "This request is taking longer than expected…" ENGLISH. Preview Emails: save toast "E-Mail-Einstellungen erfolgreich gespeichert." LOCALIZED ✓ (config restored). Upload Points: functional (selects/upload/preview); **UP#1 (P3)** "Preview" modal title ENGLISH (button "Vorschau" localized), not submitted. Test data: 1 Linked-Content item "QA localization test — please delete" (cleanup pending). Remaining: Add Employees + Create Event create toasts, Announcement publish, read-only functional (Reports export/Manage edit).
- 2026-07-22 — **Dynamic-flow pass (validation + toasts), German, live submits (user-authorised incl. outward; minimised to admin-only recipients + test data).** Validation = preventive everywhere (disabled-submit `aria-disabled` + `maxlength`; no error strings). Toasts LOCALIZED: Publish Notifications ("✓ Benachrichtigung an 1 Benutzer gesendet."), Send Custom Email ("✓ E-Mail an 1 Benutzer gesendet.") — both sent to admin only. Toasts ENGLISH: Announcement delete confirm-dialog ("Are you sure you want to delete?"/…/"Cancel"/"Delete") + success toast ("Success"/"Announcement successfully deleted.") → NEW **ANN#3 (P3)**, wire-up (keys exist). Deferred (disposable-data/org-wide): Announcement publish toast, Preview-Emails save toast, Create Event/Content + Upload-Points/Add-Employees toasts, fr/es repeat. Docs: `test-cases/dynamic-flows.md`. Cleanup: deleted a few junk "Test Announcement" rows.
- 2026-07-21 — Login: resolved via `api.vantagecircle.co.in` → Login form → app → profile menu → *HR Admin Dashboard* token handshake → `/fit/overview`. (Direct dashboard URL + Microsoft SSO both dead ends.)
- 2026-07-21 — Overview Phase 1 done: module scope + APIs mapped, test cases written to `test-cases/overview.md`. Baseline screenshot `evidence/overview_es_baseline.png`.
- 2026-07-21 — Overview Phase 2+3+4 done: executed 30 cases across en/de/fr/es (evidence `overview_{en,de,fr,es}.png`); scope extended (stat row, Leadership Insights, At a Glance, Recommended Actions). **7 bugs** logged in `bugs/logs/overview.md` (P2×3, P3×4). Root cause: Overview card components render hardcoded English, don't consume the fit i18n dictionary (translations exist). Sidebar + a few CTAs localize fine.
- 2026-07-21 — **Methodology correction:** discovered an in-place language switch can leave stale strings; must verify on FRESH route loads. Re-verified Overview #1–#6 on fresh loads (still reproduce); reframed Overview #7 from "es-only preset" to "stale-until-reload" bug.
- 2026-07-21 — Create Challenge Phase 1–4 (first pass): landing + Custom builder Steps 1–2. Bugs CC#1/CC#2.
- 2026-07-21 — Create Challenge gaps CLOSED (per user request): traversed full Custom wizard in German (Info→Duration→Audience→Config→Review), **published** via template (challenge ID 25441 "Stress Free Month" — for cleanup), checked Use-Template flow + Race builder. Added **CC#3** (audience "is in" EN), **CC#4** (config activity names EN — FE/BE TBD), **CC#5** (review date values EN month names + "Week n" inconsistency + "Custom Image"). Module bugs now 5 (P2×2, P3×3). Remaining minor gaps: fr/es deep-step passes, publish success-toast text, Journey/E-Marathon/Streak builders.
- 2026-07-21 — Manage Challenges Phase 1–4 done (en baseline + de deep): listing, Edit (tabs Challenge/Participants/Teams + Update), campaign detail. Chrome/forms localize well. **Bugs:** MGC#1 (P3) card status "Ends In X Days" untranslated (FE/BE TBD); MGC#2 (P3) chatbot overlay blocks Update button (UI). **Not testable:** delete/archive/end control not found in UI (so test challenge 25441 can't be UI-deleted); update success toast transient (NV); Participants/Teams tab contents (NV). Awaiting next-module confirmation. **Challenges area now: Create ✅, Manage ✅, Past Challenges ❌ remaining.**
- 2026-07-21 — Past Challenges Phase 1–4 done (en baseline + de deep): read-only listing (cards, View only) + campaign detail. **Clean module — 0 module-specific bugs.** Chrome + "Completed"→"Abgeschlossen" + "Private"→"Privat" all localize. Only cross-module carry-overs (date format, html lang). Confirmed MGC#1 is specific to the "Ends In X Days" countdown (sibling "Completed" localizes fine). **✅ Challenges area COMPLETE (Create + Manage + Past all done).**
- 2026-07-21 — Reports done (en baseline + de deep) across ALL 6 (League, Employee, Participation, Incentivisation, Wellness Score, Redemption). Reports localize well: table headers, empty states, Export, footer all German. **Bugs:** RPT#1 (P2) filter defaults untranslated (All Countries/Departments/Genders/Age Groups/Enrolled/Active Users); RPT#2 (P2) column-selector control fully English (inconsistent w/ localized headers); RPT#3 (P3) WSR "Employee Wellness Scores" section English; RPT#4 (P3) date values non-German + inconsistent (filter MMM-D vs cell DD-MM-YYYY). **NV:** number/points/currency VALUE formatting — all reports empty for default range (re-test with data).
- 2026-07-21 — **Reports RE-RUN** with "Dieses Jahr" (This Year) range + Generate → data loaded (Incentivisation, 4 rows). Confirmed: date VALUES have **3 inconsistent EN formats** (filter MMM-D, Employee cell DD-MM-YYYY, Incentivisation cell YYYY-MM-DD) → strengthened RPT#4; **currency** renders code+integer ("INR 25","USD 1","GBP 1") — not locale-formatted → new **RPT#5** (needs product confirmation); date-range picker **presets/Apply localized but calendar (weekdays/months) English** (same as CC#2). Points only small integers (grouping still NV). Reports now 5 module bugs (P2×2, P3×3).
- 2026-07-21 — **French & Spanish deep pass done.** Method: (1) dict parity — fr.json + es.json each **991/991 keys, 0 missing, 0 empty** (only cognate values match EN) → dictionaries COMPLETE, no fr/es missing-translation bugs; (2) spot-checks (Overview, Content Library, Settings, fresh loads both langs). App renders fr + es correctly. **All German-pass bugs reproduce identically in fr + es** (language-agnostic wire-up/not-externalized) — confirmed Overview #1, CL#1/#2/#3, RPT#1, dates, Ask-VF; es gives clearest tell (summary "Artículos" vs table "Article"). **1 NEW fr-only bug: FR#1 (P3)** — Settings banner/logo size chip truncates "…recommand[ée]" (French longer; es renders clean). Recommend a fixed-width-component truncation sweep in fr. NV: exhaustive per-module fr/es click-through, fr/es dynamic flows.
- 2026-07-21 — **Manage section done (de deep, read-only): Upload Points, Add Employees, Preview Emails.** Upload Points CLEAN (0 bugs; header/wallet/country/upload-type radios/sample-download/dropzone "Zum Hochladen klicken…"/email switch/6-step guide all de). Add Employees: **AE#1 (P3)** dropzone "Click to upload or drag and drop" EN (same control is de on Upload Points → wire-up inconsistency); rest localized. Preview Emails: page chrome fully de (whole `previewEmails.*` namespace consumed incl. save/discard keys) but **PE#1 (P3)** all 9 email-type card titles+descriptions EN ("Welcome Email (Add Employee)"/"Intro to App"/"Weekly Summary"/…) — no matching FE keys → hardcoded or backend email-template metadata (FE/BE TBD). Avoided all uploads/saves (tenant-affecting). **India-server module coverage now complete** (Settings done earlier).
- 2026-07-21 — **Workforce Health done (de deep, read-only): Health Insights (BLOCKED), Wellness Score, Wellness Leagues.** Health Insights ⛔ — iframe `dash-vfit.vantagecircle.org` refused to connect (external embedded analytics, not localizable; = old blocker #13). Wellness Score: **WS#1 (P2)** heavy mixed-language — page frame + component weights + counts + empty states + AI label localize (consume `wellnessScore.*`), but stat cards ("Current Score"/"12-Month Average"/"Industry Benchmark"), all chart card titles/subtitles, legends (High/Moderate/Low), correlation cards, "Employee Wellness Scores" render EN despite `wellnessScore.sections.*` de keys existing; one mixed line "Current period breakdown • Gesamtpunktzahl: 43". Wellness Leagues: chrome localized; **WL#1 (P3)** "Based on avg daily steps over 21 days" EN; inherits **RPT#1** (filter defaults All Countries/Departments/Age Groups/Genders on BOTH pages) + **RPT#2** (column selector "Employee ID(+8 others)"). Cross-module dates EN. NV: filter-dropdown contents, export, fr/es.
- 2026-07-21 — **Engage batch done (de deep, read-only): Create Content, Create Announcement, Publish Notifications, Send Custom Email, Email Designer.**
  - **Create Content:** Linked Content form fully localized (Type shows "Artikel" — confirms CL#1 is table/filter-only). CRC#1 (P2) type-picker modal ("Create content"/"What would you like to create?"/"Linked Content"/"Health Bite") EN, not externalized. CRC#2 (P2) Bite-Size Content Builder (`/fit/create-bite-size-content`) entirely EN, 0 dict keys.
  - **Create Announcement:** de.json has FULL `announcementPage.*` (~66 keys) + `qna.announcement.*` but UI barely consumes them. ANN#1 (P2) landing/list 100% EN; ANN#2 (P2) create form MIXED — AI+Titel+Beschreibung de, but heading/subtitle/"Audience & Delivery"/city-country/"Publish" EN. Pure wire-up.
  - **Publish Notifications:** CLEAN — 0 bugs. Attribute-audience filter localizes ("ist in"/"Alle Abteilungen") → proves EV#1/CC#3 audience widget are wire-up gaps.
  - **Send Custom Email:** page chrome clean; SCE#1 (P3/NPC) email TEMPLATE preview mixed — de placeholders + EN boilerplate ("Open Vantage Fit", "If the button above…", "Warm Regards, Vantage Fit Team", "Download the Vantage Fit app"); email locale may follow recipient — confirm w/ product.
  - **Email Designer:** ED#1 (P2) Rich Email Composer entirely EN (title, Intro/Write/Design/Send stepper, value-prop, template gallery), not externalized (like CRC#2).
  - **Pattern:** newer rich-builders (Bite-Size, Email Designer) ship with NO i18n; Announcements has full dict but no wire-up; audience multiselect (EV#1/CC#3) is wire-up. NOT executed: submit/send flows (avoided creating/emailing/publishing to tenant), deeper builder steps, fr/es, non-India.
- 2026-07-21 — **Community → Events done (en baseline + de deep, read-only).** View Events: header/subtitle/create-btn/3 tabs (Laufende/Kommende/Vergangene)/card stat labels all localized. Create Event: FULL form localized (3 section headers, every field label + required *, placeholders, gender/all-day checkboxes, upload btn, add-more links, email switch, Zurücksetzen/Neue Veranstaltung erstellen). **2 bugs:** EV#1 (P2) target-audience dropdowns (Land/Stadt/Altersgruppe/Abteilung) render EN control strings "All"/"All Countries"/"0 selected" though `targetAudience.filtersAll.country`="Alle Länder" exists — shared audience component, SAME root as Create Challenge CC#3; EV#2 (P3) time picker uses 12h AM/PM instead of de 24h. **Cross-module (not re-counted):** card date values EN format (CC#5/RPT#4), date-picker calendar weekdays "S M T W T F S" EN (CC#2), html-lang (Overview #4), Ask-VF widget (CL#4). **NV:** validation/submit-toast (avoided creating tenant event), event detail view, per-tab empty states, fr/es. Create Announcement is a separate pending module.
- 2026-07-21 — **Programs → Content Library done (en baseline + de deep, read-only).** Listing page: header/subtitle/search-placeholder/table-headers/"Inhalt ansehen"/Category-filter options ("Alle")/Content-Overview panel ("Artikel/Videos/Podcasts/Bite Size") all localized. **3 bugs:** CL#1 (P2) content-type labels render EN in Type filter (All/Article/…) + table "Typ" column ("Article") though `contentLibrary.types.*`=Alle/Artikel exist & summary panel uses them (FE wire-up, like Overview #1); CL#2 (P3) Category filter trigger button shows "All" while its options localize to "Alle"; CL#3 (P3) Bite-Size "N language(s)" badge hardcoded EN (no plural key). **Observations:** CL#4 (P4) global Ask-Vantage-Fit widget EN; CL#5 (P4 a11y) action icon buttons unlabeled. Classified via dict `/assets/i18n/fit/{de,en}.json`. **NV:** search/empty-state, filter-apply functional + count refresh, row action-menu (avoided mutating tenant rows), fr/es deep pass. Cross-module `<html lang>` (Overview #4).
- 2026-07-21 — **Configuration → Settings done (en baseline + de deep).** Scoped to the Settings page only (Add-Employees & Preview-Emails are separate modules). 3 cards (E-Mail/Challenge/App Settings): all headings, subtitles, toggle labels+descriptions, requirement chips, buttons fully localized. Dynamic surfaces exercised on UAT then reverted: **save bar** ("Sie haben nicht gespeicherte Änderungen"/"Verwerfen"/"Einstellungen speichern") localized; **Remove→default** shows localized "Standard" badge + "Eigenes Banner hochladen"; **team-size tooltip** "Min.: 5 · Max.: 500 Mitglieder pro Team" localized; number input **silently clamps** to max (no message); no text save-toast (bar disappears = feedback). **CLEAN — 0 module bugs.** Observations: SET#1 (P4/NPC) language-switcher options render in English regardless of UI lang (global sidebar element); SET#2 (P4 a11y) info icon unlabeled/hover-only. Cross-module `<html lang>` stuck "en" reproduces (Overview #4). **NV:** fr/es deep pass, upload format-error message (no asset uploaded). Login re-established this run via HR Admin Dashboard token handshake after the prior session left an orphaned Chrome holding the Playwright profile lock (freed + relaunched, login preserved).
